# Changelog

## v0.0.4 — Sanity tests pass
- Verified tables and views; inserted a test review; confirmed analytics views respond.

## v0.0.3 — Product catalogue import
- Seeded 30 synthetic SKUs; enforced lowercase enum values for `product_form`.

## v0.0.2 — Sentinel profile + consent seed
- Added SYSTEM_CONSENT profile and v1 consent text (fixes FK for consent logs).

## v0.0.1 — Initialise DB schema
- Enums, tables, triggers, RLS, and analytic views (`product_rating_stats`, `product_badges`).